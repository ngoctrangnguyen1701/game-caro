# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
env:
  REACT_APP_API_KEY: ${{secrets.REACT_APP_API_KEY}}
  REACT_APP_AUTH_DOMAIN: ${{secrets.REACT_APP_AUTH_DOMAIN}}
  REACT_APP_PROJECT_ID: ${{secrets.REACT_APP_PROJECT_ID}}
  REACT_APP_STORAGE_BUCKET: ${{secrets.REACT_APP_STORAGE_BUCKET}}
  REACT_APP_MESSAGIN_SENDER_ID: ${{secrets.REACT_APP_MESSAGIN_SENDER_ID}}
  REACT_APP_APP_ID: ${{secrets.REACT_APP_APP_ID}}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # - name: Install NodeJS
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: '14.18.0'
      - name: Install dependencies
        run: npm install
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_GAME_CARO_73268 }}'
          channelId: live
          projectId: game-caro-73268

# https://stackoverflow.com/questions/74305339/github-actions-build-fails-for-nextjs-giving-error-error0308010cdigital-enve